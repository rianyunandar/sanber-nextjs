import Layout from "@/layout";

export default function login({}) {
  return (
    <>
      <Layout metaTitle={"Login"} metaDescription={"Login Page to full access"}>
        <div className="text-3xl font-bold underline">Login Page</div>
      </Layout>
    </>
  );
}
